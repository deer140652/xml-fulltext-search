# This folder is intentionally empty by default.
# Drop your own PubMed/PMC .xml or .nxml files here, then run:
#   python3 scripts/build_index.py
# to bake them into web/data/corpus.json + index.json.
# (Uploading files live in the browser works too, and needs no rebuild.)
